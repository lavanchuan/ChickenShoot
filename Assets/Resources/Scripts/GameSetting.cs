using UnityEngine;
public class GameSetting : MonoBehaviour{
    public static readonly int WIDTH = Camera.main.pixelWidth;
    public static readonly int HEIGHT = Camera.main.pixelHeight;

    // PLAYER SETTING
    
}